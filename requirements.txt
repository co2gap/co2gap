# Compatibility entry point: the repository's analysis environment.
#
# New installations should choose the role explicitly:
#   python -m pip install -r requirements-pi.lock
#   python -m pip install -r requirements-lab.lock
-r requirements-lab.lock
